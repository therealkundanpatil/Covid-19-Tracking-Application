import React from "react";
import styles from "./Footer.module.css";

function Footer(){
    return(
        <div className={styles.container}>
            If the above statistics doesn't scare you, nothing will. Wake up Call. 
        </div>
    );
}

export default Footer;